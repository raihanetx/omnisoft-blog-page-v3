
import React from 'react';

const AppFooter: React.FC = () => {
    return (
        <footer className="text-center mt-24">
            <h3 className="text-2xl md:text-3xl font-semibold">Ready to build your next project with Omnisoft?</h3>
            <button className="mt-6 bg-violet-600 hover:bg-violet-700 text-white font-bold py-3 px-10 rounded-lg transition-colors">Contact Us</button>
        </footer>
    );
};

export default AppFooter;
